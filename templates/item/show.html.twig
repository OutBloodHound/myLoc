{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<img src="{{ asset('uploads/img/'~ item.picture ) }}" class="" alt="...">
		<div class="">
			<h5 class="">
				{{  item.name }}
			</h5>
			<p class="">{{ item.description}}</p>
			<a href="item/{{ item.id }}" class="btn btn-primary">Emprunter</a>
		</div>
	</div>
{% endblock %}
